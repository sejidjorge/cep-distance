export type handleParamsTypes = {
    type: string,
    payload: string,
};

export type calcParamsTypes = {
    lat1: number;
    lat2: number;
    lon1: number;
    lon2: number;
};

export type AddressTypes = {
    street: string;
    neighborhood: string;
    city: string;
    state: string;
};